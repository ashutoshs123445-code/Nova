{
  "name": "nova-just-connect",
  "version": "0.1.0",
  "description": "Nova — Just Connect. Custom messaging and video calling app.",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "add:android": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
